# Core dependencies — always required.
# Versions are deliberately unpinned: install the latest.
datasets
soundfile
numpy
requests
python-dotenv
# datasets>=4 decodes audio through torchcodec, which also needs ffmpeg installed.
torchcodec
